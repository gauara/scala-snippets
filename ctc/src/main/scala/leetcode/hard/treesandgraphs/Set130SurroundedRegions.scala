/*
package leetcode.hard.treesandgraphs

// STATUS -- NOT DONE

object Set130SurroundedRegions extends App {

  def isSafe(M: Array[Array[Char]], row: Int, col: Int): Boolean = {
    val rows =  M.length
    val cols = M(0).length

    if ((row >= 0 && row < rows) &&
      (col >= 0 && col < cols)
    ) {
      true
    } else {
      false
    }
  }

  def onBorder(M: Array[Array[Char]], row: Int, col: Int): Boolean = {

  }

  def dfs(board: Array[Array[Char]], visited: Array[Array[Boolean]], row: Int, col:Int): Unit = {
    if (!isSafe(board, row, col))
      return true

    if ()



  }


  def capture(board: Array[Array[Char]]): Unit = {

  }


  def solve(board: Array[Array[Char]], visited: Array[Array[Boolean]]): Unit = {

  }

}
*/
